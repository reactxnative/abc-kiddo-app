name: Create Production Builds

on:
  push:
    branches:
      - main # Jab bhi main branch par code push hoga, ye pipeline chalegi

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: production

      